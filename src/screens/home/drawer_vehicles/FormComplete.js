import React from 'react';
import { View, Text } from 'react-native';

class FormComplete extends React.PureComponent {

    render = () => (
        <View>
            <Text> Form Complete </Text>
        </View>
    );
}

export default FormComplete;
